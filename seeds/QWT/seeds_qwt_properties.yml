version : 2

seeds:
  - name: divisions
    description: "Division seed file data"
    docs:
      show: true
    config:
      database: QWT_ANALYTICS 
      alias: STG_QWT_DIVISIONS
      schema: Staging
  - name: categories
    description: "Categories seed file data"
    docs:
      show: true
    config:
      database: QWT_ANALYTICS 
      alias: STG_QWT_CATEGORIES
      schema: Staging
  - name: shippers
    description: "Shippers seed file data"
    docs:
      show: true
    config:
      database: QWT_ANALYTICS 
      alias: STG_QWT_Shippers
      schema: Staging