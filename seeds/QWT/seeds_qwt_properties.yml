version : 2

seeds:
  - name: divisions
    description: "Division seed file data"
    docs:
      show: true
    config:
      database: QWT_ANALYTICS 
      alias: STG_QWT_DIVISIONS
      schema: Staging